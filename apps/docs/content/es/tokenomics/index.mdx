---
title: Tokenómica
searchable: false
icon: LightningIcon
asIndexPage: true
description:
  La guía de convenciones de archivos de Nextra detalla la estructura y
  organización de archivos y directorios dentro de un proyecto Nextra,
  incluido el uso de archivos `page.mdx`, `_meta.js` y
  `mdx-components.js`, así como los directorios `content` y `src`.
---

import {
  FileIcon,
  FolderIcon,
  MdxIcon,
  GlobeIcon,
  CPPIcon,
} from 'nextra/icons';
import { OverviewPage } from '@/components/overview-page';

# Tokenómica

<OverviewPage
  filePath={metadata.filePath}
  icons={{ FileIcon, FolderIcon, MdxIcon, GlobeIcon, CPPIcon }}
/>